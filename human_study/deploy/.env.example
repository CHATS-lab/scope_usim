# Copy to deploy/.env on the VPS (already gitignored). docker-compose reads
# this automatically for variable substitution in docker-compose.yml.
#
# Generate a strong password with e.g.  openssl rand -base64 32
POSTGRES_USER=usim
POSTGRES_PASSWORD=change-me-before-launch
POSTGRES_DB=usim_study
